# ============================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS POSTGRESQL
# ============================================================================
# Copie este arquivo para .env e preencha com as credenciais reais
# Comando: cp .env.example .env

DB_NAME=n8n
DB_USER=admin
DB_PASSWORD=BetaAgent2024SecureDB

# ============================================================================
# BASICAUTH PARA TRAEFIK
# ============================================================================
# Gerar hash da senha:
# apt install -y apache2-utils
# htpasswd -nb revisaprecatorio 'R3v1s@2025'
# 
# Copie o resultado completo (revisaprecatorio:$apr1$...) e cole abaixo
# IMPORTANTE: Escape os $ com $$ (ex: $apr1$ vira $$apr1$$)

BASICAUTH_HASH=revisaprecatorio:$$apr1$$HASH_AQUI

# ============================================================================
# CONFIGURAÇÃO DE PATHS
# ============================================================================
# Path para os PDFs originais (relativo ao projeto)
PDF_DIR=../data/consultas

# Path para os JSONs processados (relativo ao projeto)
JSON_DIR=../1_parsing_PDF/outputs

# ============================================================================
# CONFIGURAÇÃO DO STREAMLIT
# ============================================================================
STREAMLIT_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
